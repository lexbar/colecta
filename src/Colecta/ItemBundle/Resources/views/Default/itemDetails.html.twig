<p class="details"><a href="{{ path('userProfile', { 'id': item.author.id }) }}" class="username">{% if hideavatar is not defined %}<img src="{{ path('userAvatar',{'uid':item.author.id,'width':20,'height':20}) }}"> {% endif %}{{item.author.name}}</a> <span class="dateandcat">{% include 'ColectaItemBundle:Default:timeago.html.twig' with { 'date': item.date } only %}{% if item.category %} en <a class="category" href="{{ path('ColectaCategoryView', { 'slug': item.category.slug }) }}">{{ item.category.name }}</a>{% endif %}</span></p>